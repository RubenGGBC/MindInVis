# Docker configuration removed from repository.
# This project now runs locally. Use the workspace scripts:
# - npm run dev (from repo root) to start client and server in development.
# - cd server && npm run start to run the server alone.
# Remove this file if desired.